import React, { Fragment } from 'react';
import MainBanner from '../Components/Web/MainBanner';

const Home = () => {
	return (
		<Fragment>
			<MainBanner />
		</Fragment>
	);
};

export default Home;
